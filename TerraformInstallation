sudo yum install -y yum-utils
sudo yum-config-manager --add-repo https://rpm.releases.hashicorp.com/AmazonLinux/hashicorp.repo
sudo yum -y install terraform

-- Access AWS CLI--
--Access key and Secret Key --
Go to IAM-> Users-> Click on an user -> Security Credentials-> Access Keys -> Generate Access keys
Go to AWS instance -> Root user -> type aws config -> Enter the access keys and region
